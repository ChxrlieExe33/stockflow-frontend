export type SaleInfoModel = {
    orderId: string,
    reference: string,
    deliveryDate: Date,
    deliveryAddress: string,
    deliveryPhoneNumber: string,
    extraInformation: string,
    orderedAt: Date,
    soldBy: string,
    delivered: boolean
}
