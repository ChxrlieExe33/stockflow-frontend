export type NewProductInstanceModelWithName = {
    rootProductId: string,
    rootProductName: string,
    width: number | null,
    length: number | null,
    height: number | null,
    colour: string | null,
    reserved: boolean
}
